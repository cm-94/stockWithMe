import 'dart:ui';

class MyColors {
  // Natural
  static Color bgWhite = const Color(0xFFBFBFBF);
  static Color bgTransparent = const Color(0x00000000);

  // static Color themeColor({required Color light, required Color dark}) {
  //   final global = Get.find<GlobalController>();
  //   final isDark = global.theme == ThemeType.DARK;
  //
  //   return isDark ? dark : light;
  // }
}